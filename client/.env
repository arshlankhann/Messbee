# Messbee Frontend - Environment Variables

# API Base URL (backend origin — no trailing /api)
# contact.jsx and importContact.jsx append /api/... paths themselves.
# axios.jsx uses its own fallback: http://localhost:5000/api
VITE_API_URL=http://localhost:5000
VITE_MAX_FILE_SIZE=5242880

